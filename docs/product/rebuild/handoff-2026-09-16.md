---
title: "Dossier 0.7 rebuild handoff"
slug: "handoff-2026-09-16"
status: "current"
updated: "2026-09-16"
---

# Dossier 0.7 rebuild handoff

Written to transfer an in-progress session. Read this, then
`dossier-0-7-rebuild-plan.md` beside it, then `core/AGENTS.md`. Everything
below is settled unless it appears under **Open questions**; do not relitigate
a decision here without Kyle saying so.

## Where things stand in one paragraph

Dossier 0.6.7 is a Node generator that turns one JSON model into a
self-contained interactive HTML file. It grew bloated across the 0.5 and 0.6
arcs. Kyle decided to rebuild the core from scratch in Go under `core/`,
following the conventions of his `kore` stack and shaped like his `corbelo`
CLI, while the Node tree in `src/` keeps shipping untouched until cutover.
Milestones M0 through M3 are done and committed, plus two rounds of design
polish. The design being finalized here is also the brand style that will be
applied to Ledger next.

## Decision history, so it is not reopened

1. **Rebuild, not reshape.** All twelve moves from the 0.7 brainstorm are in
   scope. Kyle picked the rebuild path: a new core beside the old tree, cut
   over when every example builds through it, then delete the old tree.
2. **Go, on the kore conventions, shaped like corbelo.** Not TypeScript. The
   reasons: one CGO-free static binary, Homebrew distribution, a real
   self-hosting story, and Kyle wants his own AI-optimized stack used and
   refined here. `kore` is a convention reference and a future source of
   copy-in capabilities; Dossier is a standalone module and does **not**
   adopt kore's composer, registry, or plugin machinery.
3. **Same repo, new directory.** `core/` beside `src/`.
4. **The name stays Dossier.** Kyle typed "Ledger" once; he meant Dossier.
5. **The design system here is the shared brand style.** Ledger
   (`/Users/kyle/Developer/active/ledger`, a TypeScript project) gets the
   same tokens, fonts, and components once this is signed off.

## Repository state

Git was initialized in the first session (the checkout had no repository).
Branch `main`, clean tree, **no remote configured**. Commits end with the
co-author line of the model that wrote them. `git log --oneline` is the
record; the milestone boundaries are:

| Milestone | Last commit |
| --- | --- |
| M0 to M3, design polish | `f81bd43 docs: add the 0.7 rebuild handoff` |
| M4 | `4c118cf docs: record M4 as done and the diagram and preset decisions` |
| M5 | `414f0de docs: record M5 as done` |
| M6 | the docs commit recording M6 as done |

## The stack

Go 1.26, module `dossier`, stdlib `flag` and `net/http`. Dependencies:
`a-h/templ` for HTML, `yuin/goldmark` for markdown, `santhosh-tekuri/jsonschema/v6`
for schema validation, `alecthomas/chroma/v2` for build-time highlighting,
`modelcontextprotocol/go-sdk` for the `mcp` door, and `modernc.org/sqlite`
with `pressly/goose/v3` for the studio store. Tools are pinned with `go.mod`
tool directives (`templ`, `sqlc`, `staticcheck`, `errcheck`); sqlc runs
CGO-free. `packages/react` uses TypeScript 7 and React 19 as dev dependencies
and tests with `node --test`.

## The model: five concepts

- **Kind** is a preset naming the facets items carry, the field vocabularies,
  the summary table columns, how contents group, and the closing pick text.
  `brainstorm` carries the full vocabulary; `plan`, `review`, `release`,
  `incident`, and `brief` are permissive presets pending their vocabularies.
- **Section** has a title and either content parts or a board. Part types:
  `prose`, `spec`, `table`, `callout`, `code` (highlighted), `figure`,
  `diagram` (source only), `chart`.
- **Item** is a numbered thing in a board with id, title, summary, size,
  effort, impact, dependsOn, facets. Board layout `rows` means unnumbered
  also-considered entries.
- **Facet** is a labeled markdown body, validated against the kind's fixed
  order. Risk and Note render with the ochre label.
- **Decision** is the only state: path, picked ids, notes by id.

## Rules the library enforces

- **Effort is agent time, never calendar time.** `S` under an hour, `M` a few
  hours with review, `L` a day or more across sessions. No days-of-work or
  weeks-of-work language in presets, examples, docs, or the skill. This came
  from Kyle directly: building is fast now; what varies is model and tokens.
- **Concise by default.** One-sentence summaries, two- or three-sentence
  facets. Kinds carry character limits (160 and 420 for brainstorm) reported
  as **warnings** in the result envelope by `validate` and `build`. Warnings
  never block a build.
- **Detail sits behind a fold.** Items render collapsed.
- **The artifact makes zero external requests** unless `meta.fonts: "google"`.
- **Budgets are tests.** Reader runtime at most 20 KB (currently 11 KB),
  stylesheet at most 30 KB (currently 20 KB), artifact under 120 KB
  (currently 94 KB).

## Done

- **M0.** Module, `Makefile`, `AGENTS.md`, `docs/llm/START_HERE.md`,
  `scripts/check-generated.sh`, `make check` green.
- **M1, mostly.** Model with strict decoding, JSON Schema, structure checks,
  kind rules including facet order, `validate` door. **Not done:** aliases for
  0.6 block type names.
- **M2, mostly.** templ renderer, tokens, reader runtime, `build` door. The
  brainstorm builds in about 2 ms. **Not done:** syntax highlighting, figure,
  diagram, chart parts.
- **M3, done.** `internal/decisions` (dossier.decisions/v1 as Markdown with
  fenced JSON or as JSON, the reply line, a reply parser, apply with
  findings), `decisions read` and `decisions apply` doors, decided state
  rendered into the artifact, reader notes, copy as JSON. Round trip is
  covered by a test: apply a reply, build, read back, apply to a fresh model.
- **Two design rounds.** Full-width masthead band with a slim stat strip,
  collapsible items with per-item memory and Collapse all / Expand all, a
  Hide picked filter with a hidden-count row at the end of the board, a
  1360px frame with a 76ch measure, a tightened contents rail with numbered
  entries ending in a period, one button style, consistent spacing, footer
  breathing room. Two real bugs fixed: buttons in an item head toggled the
  item, and print expansion overwrote saved collapse state.

- **M4, done.** `figure`, `diagram`, and `chart` parts; chroma highlighting
  (classed spans, hand-written token theme in `tokens.css`, fenced code in
  markdown included); relative figure paths inlined by the build door while
  the model island keeps the path; rows boards fold their facets. Five
  permissive presets (`plan`, `review`, `release`, `incident`, `brief`).
  `internal/model/alias.go` rewrites any 0.6 document onto the model with a
  warning per aliased block; the seventeen fixtures in `core/testdata/legacy`
  build with outcome `ok`. Decided with Kyle: diagrams emit source until a
  wasm Graphviz is adopted as its own decision; presets get their facet
  vocabularies in M5.

- **M5, done.** `init` (starter per kind, refuses to overwrite without
  `--force`), the `dossier.result/v1` envelope schema (checked on every door
  test envelope), catalog `positional` markers, `mcp` over stdio through the
  official Go SDK (in-memory and real-binary conformance tests), and `skill`
  generating `core/skill/SKILL.md` with a drift test. The old root `skill/`
  stays until cutover.

- **M6, done except deletion and publishing, which wait for Kyle.**
  `internal/load` (one read pipeline, one atomic writer), `upgrade`, `render`,
  and `types` doors, `--version`, a content-based drift script and a gofmt
  gate, `internal/store` on kore's data layer, and `serve` with the studio
  island: drafts keyed by stable ids with base checks, reorder, decisions
  sync and write, a validated Model JSON editor, and an accent preview,
  verified in the browser. The reader posts height and decisions when framed.
  `packages/react` wraps the output with generated model types.
  `internal/release` builds reproducible archives, npm packages, and the
  Homebrew formula; `make dist-check` dry-runs all of it. The README, the
  screenshots, `manual-qa-0-7.md`, and `cutover.md` are written.

## Next

- **Cutover.** Follow `docs/product/rebuild/cutover.md` when Kyle says go:
  delete the 0.6 tree, upgrade the examples, then publish.
- **Ledger.** Apply the same tokens and components.

## How to verify

```sh
cd core
make check      # generate, drift check, vet, staticcheck, errcheck, race tests, CGO-free build
go run ./cmd/dossier build examples/dossier-0-7-brainstorm.dossier.json --out examples
go run ./cmd/dossier validate FILE --json
go run ./cmd/dossier init brainstorm --title "My brainstorm" --out /tmp/out
go run ./cmd/dossier build testdata/legacy/showcase.dossier.json --out /tmp/out   # a 0.6 file, warnings only
go run ./cmd/dossier mcp            # MCP over stdio; go test ./internal/doors/ runs the conformance test
UPDATE_GOLDEN=1 go test ./internal/render/ ./internal/model/   # after a deliberate render or import change
```

## Gotchas learned the hard way

- **templ cannot evaluate expressions inside `<style>` or `<script>`.** The
  stylesheet, the model island, and the reader script are assembled as
  strings in `render.go` and emitted with `templ.Raw`.
- **`make check` fails if generated `*_templ.go` is dirty in git.** Run
  `make generate` and commit the output.
- **errcheck rejects bare `fmt.Fprintf` to an `io.Writer`.** Use the `say` and
  `sayln` helpers in `internal/doors/doors.go`.
- **staticcheck ST1005:** error strings must not end with punctuation.
- **errcheck flags `defer x.Close()`.** Write `defer func() { _ = x.Close() }()`.
- **`make check` fails while generated output is untracked or dirty.** That
  covers `*_templ.go` and `skill/SKILL.md`; commit them with the change.
- **Piping `make check` through `grep | tail` hides its exit status.** Check
  `$?` or redirect to a file first.
- **go vet:** a non-constant format string in a printf-style wrapper is an
  error; write `say(w, "%s", x)`.
- **The browser pane cannot drive local files.** Serve them over loopback
  (`python3 -m http.server` or `dossier serve`) and navigate to that URL.
  Its "Return" key arrives with an empty key; send "Enter".
- **Headless Chrome screenshots follow the system theme and may not exit.**
  Pass `--blink-settings=preferredColorScheme=1` for light, run it in the
  background, and stop the process you started once the PNG exists.
- **The in-app browser preview serves stale snapshots of local files** and
  disables `localStorage` on `data:` URLs. To look at a rebuilt artifact,
  write it under a fresh filename or close and reopen the tab, and verify
  persistence by reading the code rather than in the pane.

## Files that matter

| Path | What it is |
| --- | --- |
| `docs/product/rebuild/dossier-0-7-rebuild-plan.md` | The plan, milestones, status |
| `core/AGENTS.md` | Layout map and the content rules |
| `core/docs/llm/START_HERE.md` | Task router for the module |
| `core/internal/render/assets/tokens.css` | **The brand design system** |
| `core/internal/render/document.templ` | Page structure |
| `core/internal/kinds/presets/brainstorm.json` | The only kind so far |
| `core/examples/dossier-0-7-brainstorm.dossier.json` | The reference document |
| `core/examples/dossier-0-7-brainstorm.html` | Current rendered design |
| `docs/scratchpad/dossier-0-7-brainstorm.html` | Hand-built original; **now behind** the Go render |

The published artifact at https://claude.ai/artifact/3d71VawLLNY3d8p7D1guRT is
the hand-built page and is also behind the current design.

## Open questions for Kyle

1. **Accent color.** Crimson is the default; blue was the alternative from the
   front door reference. Flagged as a risk on item 3 and never settled. The
   studio's accent preview (`dossier serve`) now lets Kyle try both live.
2. **Is the hide-picked filter universal?** It was built as a general reader
   behavior for any board with picks. Kyle was unsure whether to generalize it
   or keep it specific; it is currently general but undocumented as a component.
3. **Stats and details as disclosures with charts or detail pages.** Kyle
   raised it and explicitly held off. The collapsible item and the hidden row
   are the building blocks when he wants it.
4. **Git remote.** None configured. Nothing has been pushed anywhere.
5. **Pages site.** The live demo builds from `src/`; rebuild it from
   `dossier build` or drop it at cutover.
6. **Facet vocabularies for the five new presets.** They accept any labels
   today and were not designed in M5. When they are, imported 0.6 documents
   need the alias pass to emit only known labels, or the presets stay open for
   imports until 0.8 removes the aliases.
7. **Wasm Graphviz.** Deferred; `diagram` renders source. Adopting
   `goccy/go-graphviz` adds a multi-megabyte wasm and a compile on first use.
